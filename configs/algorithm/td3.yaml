name: TD3
params:
  batch_size: 256
  gamma: 0.99
  learning_rate: 0.001
  learning_starts: 100
  policy_kwargs: {}
  implementation: sb3  # Options: "custom", "sb3", "other"
  policy: "MlpPolicy" # only for sb3
  eval_interval: 5000
  policy_kwargs: {}
  gradient_steps: 1